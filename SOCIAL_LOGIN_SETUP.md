# 소셜 로그인 설정 가이드

## 📋 구현 완료된 기능

✅ **구글 로그인** - Firebase Auth와 연동
✅ **카카오 로그인** - Kakao SDK 연동  
✅ **애플 로그인** - Sign in with Apple 연동
✅ **UI 구현** - 각 서비스별 버튼과 아이콘
✅ **에러 처리** - 각 서비스별 오류 메시지

## 🔧 설정 방법

### 1. Firebase 프로젝트 설정

1. [Firebase Console](https://console.firebase.google.com/)에서 프로젝트 생성
2. Authentication > Sign-in method에서 다음 활성화:
   - **Google** (웹 클라이언트 ID 필요)
   - **Apple** (iOS에서만 사용 가능)

### 2. 구글 로그인 설정

#### Android 설정
1. `android/app/google-services.json` 파일을 실제 Firebase 프로젝트에서 다운로드한 파일로 교체
2. `android/app/src/main/res/values/strings.xml`에서 `YOUR_GOOGLE_WEB_CLIENT_ID`를 실제 웹 클라이언트 ID로 교체

#### iOS 설정  
1. `ios/Runner/GoogleService-Info.plist` 파일을 실제 Firebase 프로젝트에서 다운로드한 파일로 교체

### 3. 카카오 로그인 설정

1. [Kakao Developers](https://developers.kakao.com/)에서 앱 등록
2. `lib/main.dart`에서 `YOUR_KAKAO_NATIVE_APP_KEY`를 실제 네이티브 앱 키로 교체
3. `android/app/src/main/res/values/strings.xml`에서 `YOUR_KAKAO_NATIVE_APP_KEY`를 실제 키로 교체

### 4. 애플 로그인 설정 (iOS만)

1. Apple Developer Console에서 Sign in with Apple 활성화
2. Bundle ID와 Team ID 설정
3. Firebase Console에서 Apple 로그인 활성화

## 📱 사용법

### 로그인 화면에서
- **구글 버튼**: 구글 계정으로 로그인
- **카카오 버튼**: 카카오 계정으로 로그인  
- **애플 버튼**: Apple ID로 로그인 (iOS만)

### 로그인 성공 시
- 자동으로 사용자 정보가 Firestore에 저장됨
- 타임라인 화면으로 이동
- 가족 등록이 필요한 경우 가족 등록 화면으로 이동

## 🔍 코드 위치

- **AuthService**: `lib/services/auth_service.dart`
- **UI 버튼**: `lib/widgets/common_screen_layout.dart` (189-225행)
- **패키지 설정**: `pubspec.yaml`
- **초기화**: `lib/main.dart`

## ⚠️ 주의사항

1. **실제 키 교체 필수**: 모든 `YOUR_*` 플레이스홀더를 실제 값으로 교체
2. **카카오 로그인**: 현재 임시 구현으로 실제 운영 시 서버에서 커스텀 토큰 생성 필요
3. **애플 로그인**: iOS에서만 작동하며 실제 디바이스에서 테스트 필요
4. **Firebase 보안 규칙**: Firestore 보안 규칙 설정 필요

## 🚀 테스트 방법

1. `flutter pub get` 실행
2. 실제 디바이스에서 테스트 (에뮬레이터에서는 일부 기능 제한)
3. 각 소셜 로그인 서비스별로 테스트 계정 준비
4. Firebase Console에서 사용자 생성 확인

## 📞 문제 해결

- **구글 로그인 실패**: SHA-1 지문 확인 및 Firebase 설정 재확인
- **카카오 로그인 실패**: 네이티브 앱 키 및 패키지명 확인
- **애플 로그인 실패**: Bundle ID 및 Apple Developer 설정 확인
